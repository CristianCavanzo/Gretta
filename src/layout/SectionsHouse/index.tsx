import React from 'react';
import styled from 'styled-components';
const Section = styled.section``;
const SectionHouse = ({ children }) => {
	return <Section>{children}</Section>;
};

export { SectionHouse };
